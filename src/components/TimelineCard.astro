---
interface Props {
  title: string;
  subtitle: string;
  year: string;
  description?: string;
  skills?: string[];
  index: number;
}

const { title, subtitle, year, description, skills, index } = Astro.props;
---

<div class="relative pl-12 pb-12 last:pb-0">
  <div class="absolute left-0 top-0 w-8 h-8 rounded-full bg-primary/10 flex items-center justify-center">
    <div class="w-4 h-4 rounded-full bg-primary"></div>
  </div>
  
  <div class="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition-shadow duration-300">
    <div class="flex flex-col md:flex-row md:items-center md:justify-between mb-4">
      <div>
        <h3 class="text-xl font-semibold text-gray-900">{title}</h3>
        <p class="text-primary font-medium">{subtitle}</p>
      </div>
      <span class="text-sm text-gray-500 mt-2 md:mt-0">{year}</span>
    </div>
    
    {description && <p class="text-gray-600 mb-4">{description}</p>}
    
    {skills && (
      <div class="flex flex-wrap gap-2">
        {skills.map((skill) => (
          <span class="px-3 py-1 bg-primary/5 text-primary rounded-full text-sm">
            {skill}
          </span>
        ))}
      </div>
    )}
  </div>
</div> 